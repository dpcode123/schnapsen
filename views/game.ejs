<%- include('partials/head') %>

<body>

    <div class="svg-container">
        <svg id="svg-content" viewBox="0 0 1080 540" xmlns="http://www.w3.org/2000/svg"></svg>
        <div class="modal">
            <div class="modal-content">
                <span class="modal-close-button">×</span>
                <h1>Back to main menu?</h1>
                <a id="modal-exit-game-btn" class="modal_button" title="Back to main menu" href="/">Yes</a>
                <a id="modal-continue-game-btn" class="modal_button" title="Continue game" href="#">No</a>
            </div>
        </div>
    </div>

    <svg id="card_patterns" xmlns="http://www.w3.org/2000/svg">
        <defs id="defs">
        </defs>
    </svg>    

    <script>
        const passedUsername = JSON.parse('<%- JSON.stringify(username) %>');
        const passedUserId = JSON.parse('<%- JSON.stringify(userId) %>');
        const passedRoom = JSON.parse('<%- JSON.stringify(roomId) %>');
        const passedSocketJwt = JSON.parse('<%- JSON.stringify(token) %>');
        const passedUserCardFace = JSON.parse('<%- JSON.stringify(userCardFace) %>');
        const passedUserCardBack = JSON.parse('<%- JSON.stringify(userCardBack) %>');
    </script>

    <script src="/socket.io/socket.io.js"></script>
    <script src="js/script.js"></script>
    
</body>

<%- include('partials/footer') %>